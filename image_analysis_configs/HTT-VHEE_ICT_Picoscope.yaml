# VHEE ICT 
# Configuration for measuring charge from oscilloscope traces using ICT1DAnalyzer

name: "HTT-VHEE_ICT_Picoscope"
description: "VHEE ICT charge measurement from oscilloscope traces"
data_format: "volts (V) vs time (s)"

# Data loading configuration
data_loading:
  data_type: "tdms_scope"
  trace_index: 0

# Data type settings
processing_dtype: "float64"
storage_dtype: "float32"

# Background subtraction configuration
background:
  method: "constant"
  constant_value: 0.0

# Filtering configuration
filtering:
  method: "none"

# Thresholding configuration
thresholding:
  method: "none"

# Pipeline configuration - empty for ICT (handled by analyzer)
pipeline:
  steps: []

ict_options:
  crit_freq: 0.125
  order: 1
  dt: 2e-9 #s
  calib: 0.2 #C/Vs
